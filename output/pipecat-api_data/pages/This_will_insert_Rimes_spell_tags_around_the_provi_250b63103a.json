{
  "title": "This will insert Rime's spell tags around the provided text.",
  "content": "async def spell_out_text(text: str, type: str) -> str:\n    return RimeTTSService.SPELL(text)\n\ntts = RimeTTSService(\n    api_key=os.getenv(\"RIME_API_KEY\"),\n    text_transforms=[\n        (\"phone_number\", spell_out_text),\n    ],\n)\npython theme={null}",
  "code_samples": [
    {
      "code": "### PAUSE\\_TAG(seconds: float) -> str:\n\nImplements [Rime's custom pause functionality](https://docs.rime.ai/api-reference/custom-pauses) to generate a properly formatted pause tag you can insert into the text.",
      "language": "unknown"
    }
  ],
  "headings": [
    {
      "level": "h3",
      "text": "PAUSE\\_TAG(seconds: float) -> str:",
      "id": "pause\\_tag(seconds:-float)-->-str:"
    }
  ],
  "url": "llms-txt#this-will-insert-rime's-spell-tags-around-the-provided-text.",
  "links": []
}